cmake_minimum_required(VERSION 3.5)
set(COMPONENTS "main retro-go gwenesis")
set(RG_ENABLE_NETWORKING 0)
include(../base.cmake)
if(DEFINED ENV{GEN_PROF} AND NOT "$ENV{GEN_PROF}" STREQUAL "0")
    idf_build_set_property(COMPILE_DEFINITIONS "-DGEN_PROF=1" APPEND)
    message(STATUS "GEN_PROF=1: gwenesis frame profiling enabled")
endif()
project(gwenesis)
